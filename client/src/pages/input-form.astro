---
/* 
const response = await fetch('http://127.0.0.1:8000/api/get-items')
const data = await response.json();
console.log(data)
const items = data.items[0];
*/
---
<form action="#" method="post">
    <input type="text" name="name" placeholder="some random text here">
    <button type="submit">Submit</button>
</form>
<!--- 
    <items.map(item => {
        return (
            <div>
                <h1>{item.name}</h1>
                <p>{item.description}</p>
            </div>
        )
    })
--->
<!--- 
<div class="overflow-x-auto">
  <table class="table">
   
    <thead>
        <tr>
          <th></th>
          <th>Item</th>
          <th>Descirption</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <th>{items.item}</th>
          <td>{items.description}</td>
        </tr>
      </tbody>
    </table>
  </div>
    <div>
        <h1>Items</h1>
        <section>
            <h1>{items.name}</h1>
            <p>{items.description}</p>
        </section>
    </div>
-->